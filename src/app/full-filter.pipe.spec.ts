import { FullFilterPipe } from './full-filter.pipe';

describe('FullFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FullFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
