import { SmartSearchPipe } from './smart-search.pipe';

describe('SmartSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SmartSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
